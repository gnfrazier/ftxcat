[project]
name = "ftxcat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyserial>=3.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]

[tool.ruff.lint]
extend-select = ["B"]